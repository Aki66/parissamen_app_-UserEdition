name: parissamin_app
description: COVID-19 Associate Tracker
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.1.1
  firebase_core: ^1.0.2
  firebase_auth: ^1.0.1
  google_sign_in: ^5.0.1
  cloud_firestore: ^1.0.3
  google_fonts: ^2.0.0
  qr_flutter: ^4.0.0
  qr_code_scanner: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

 

flutter:
  uses-material-design: true

  assets:
    - assets/images/Logouser.png 